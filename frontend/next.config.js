/** @type {import('next').NextConfig} */
const nextConfig= {
  /* config options here */
  images: {
    remotePatterns: [{
      hostname: "chematsustain.eu"
    }],
  },
};

export default nextConfig;